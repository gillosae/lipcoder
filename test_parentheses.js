// Test file for parentheses sound in line reading
function testFunction(param1, param2) {
    console.log("Hello world");
    if (condition) {
        return getValue(x, y);
    }
    const result = calculate(a + b, c * d);
    array.push(item);
}

// More test cases
const obj = { key: getValue() };
const arr = [1, 2, func(3)];
